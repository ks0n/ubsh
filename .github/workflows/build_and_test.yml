name: ubsh-build-test

on:
  push:
      branches:
        - main

  pull_request:
    branches:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: alexisehret/archlinux_c_meson:latest

    steps:
      - uses: actions/checkout@v2
        with:
          submodules: 'recursive'
      - name: Build ubsh
        run: |
          yay criterion
          meson build
          ninja -C build

  tests:
    runs-on: ubuntu-latest
    container:
      image: alexisehret/archlinux_c_meson:latest
    needs: build

    steps:
      - uses: actions/checkout@v2
        with:
          submodules: 'recursive'
      - name: Run Unit Tests
        run: |
          yay criterion
          meson build
          ninja -C build test
